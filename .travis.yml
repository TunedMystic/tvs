matrix:
  include:
    - language: python
      python:
        - "3.7-dev"
      before_install:
        - cd backend/
      install:
        - pip install pipenv
        - pipenv install
      script:
        - pipenv run pytest tests
